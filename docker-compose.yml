version: '3.5'

services:
  proxy:
    build: proxy/
    ports:
      - "80:80"
  frontend:
    build: .
  robokache:
    image: docker.pkg.github.com/ncats-gamma/robokache/build:v4.1.1
    volumes:
      - ./robokache-data:/app/data
